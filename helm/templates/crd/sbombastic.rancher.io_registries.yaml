---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: registries.sbombastic.rancher.io
spec:
  group: sbombastic.rancher.io
  names:
    kind: Registry
    listKind: RegistryList
    plural: registries
    singular: registry
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Registry is the Schema for the registries API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RegistrySpec defines the desired state of Registry
            properties:
              authSecret:
                description: |-
                  AuthSecret is the name of the secret that contains the credentials to access the registry.
                  The Secret is expected to be in the same namespace as the Registry.
                type: string
              caBundle:
                description: CABundle is the CA bundle to use when connecting to the
                  registry.
                type: string
              insecure:
                description: Insecure allows insecure connections to the registry
                  when set to true.
                type: boolean
              repositories:
                description: Repositories is the list of the repositories to be scanned
                items:
                  type: string
                type: array
              url:
                description: URL is the URL of the container registry
                type: string
            type: object
          status:
            description: RegistryStatus defines the observed state of Registry
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
