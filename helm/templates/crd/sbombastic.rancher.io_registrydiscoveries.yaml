---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: registrydiscoveries.sbombastic.rancher.io
spec:
  group: sbombastic.rancher.io
  names:
    kind: RegistryDiscovery
    listKind: RegistryDiscoveryList
    plural: registrydiscoveries
    singular: registrydiscovery
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RegistryDiscovery is the Schema for the registrydiscoveries API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RegistryDiscoverySpec defines the desired state of RegistryDiscovery.
            properties:
              registry:
                description: registry name in the same namespace
                type: string
              registrySpec:
                description: registry spec
                properties:
                  authSecret:
                    description: AuthSecret is the name of the secret in the same
                      namespace that contains the credentials to access the registry.
                    type: string
                  caBundle:
                    description: CABundle is the CA bundle to use when connecting
                      to the registry.
                    type: string
                  discoveryJob:
                    description: DiscoveryJob is the configurations for scheduled
                      discovery
                    properties:
                      cron:
                        description: cron in numeric format. see https://en.wikipedia.org/wiki/Cron
                        properties:
                          dayOfMonth:
                            description: 1~31
                            maximum: 31
                            minimum: 1
                            type: integer
                          dayOfWeek:
                            description: '0~6: Sunday to Saturday'
                            maximum: 6
                            minimum: 0
                            type: integer
                          hour:
                            description: 0~23
                            maximum: 23
                            minimum: 0
                            type: integer
                          month:
                            description: 1~12
                            maximum: 12
                            minimum: 1
                            type: integer
                        type: object
                      failedJobsHistoryLimit:
                        default: 1
                        description: number of RegistryDiscovery objects with failed
                          state to keep
                        maximum: 2
                        minimum: 0
                        type: integer
                      successfulJobsHistoryLimit:
                        default: 1
                        description: number of RegistryDiscovery objects with successful
                          state to keep
                        maximum: 2
                        minimum: 0
                        type: integer
                      suspend:
                        description: Suspend scheduled discovery
                        type: boolean
                    required:
                    - cron
                    - failedJobsHistoryLimit
                    - successfulJobsHistoryLimit
                    - suspend
                    type: object
                  insecure:
                    description: Insecure allows insecure connections to the registry
                      when set to true.
                    type: boolean
                  repositories:
                    description: |-
                      Repositories is the list of the repositories to be scanned
                      An empty list means all the repositories found in the registry are going to be scanned
                    items:
                      type: string
                    type: array
                  uri:
                    description: URI is the URI of the container registry
                    minLength: 1
                    type: string
                required:
                - discoveryJob
                - uri
                type: object
            required:
            - registry
            - registrySpec
            type: object
          status:
            description: RegistryDiscoveryStatus defines the observed state of RegistryDiscovery.
            properties:
              canceled:
                description: registry discovery is canceled
                type: boolean
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              currentStatus:
                description: current status of the registry discovery
                type: string
              finishedAt:
                description: timestamp of the stop handling time
                type: string
              startedAt:
                description: timestamp of the start handling time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
